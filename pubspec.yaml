name: zephyr
description: zephyr by claret

publish_to: "none"

version: 2.2.5+1

environment:
  sdk: ">=3.0.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences:
  dynamic_color:
  dio:
  dio_cache_interceptor: 
  geolocator:
  geocoding:
  home_widget:
  url_launcher:
  intl:
  android_battery_optimizations:
  animations:
  fl_chart:
  flutter_map: 
  flutter_map_cache: 
  http_cache_file_store: 
  latlong2: 
  path_provider: 
  percent_indicator:
  flutter_colorpicker:
  json_annotation:
  workmanager:
  flutter_local_notifications:
  timezone:
  permission_handler:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:
  json_serializable:
  build_runner:

flutter:
  generate: true
  uses-material-design: true
